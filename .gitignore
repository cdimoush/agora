
# Beads
.beads/
.dolt/
.beads-credential-key

# Secrets
.env
.env.*

# Testbed secrets (tokens are per-bot, never committed)
testbed/*/.env

# Telemetry logs
logs/

# Python
__pycache__/
*.egg-info/
.venv/
.pytest_cache/

# Claude Code
.claude/
# But keep testbed citizen configs
!testbed/citizen-a/.claude/
!testbed/citizen-b/.claude/

# Beads / Dolt files (added by bd init)
*.db

# Fleet — agent instances (generated, not tracked)
fleet/

# Docker compose — derived from fleet/ contents
docker-compose.yml
docker-compose.override.yml

# Worktrees — per-agent agora copies (Phase 2)
worktrees/

# Clones — per-agent full git clones (dev agent)
clones/
.perles/
